Below is a line-by-line implementation spec for /mnt/data/EmaPullback.ts (your current file is 142 lines). I’m going to stay brutally concrete: what to change, where, and exactly what code to drop in.

A) Summary Verdict

Overall quality of existing work: Strong foundation / Mixed execution
Confidence level: High (because the bugs are real, scoped, and easy to validate)

B) What Holds Up

Trend-continuation classification is correct (strategyType: 'trend-continuation').

Preflight integration is correct (H4 alignment/counter-trend handling exists).

Stops are already structure-first (EMA zone + ATR buffer) — this is better than your MR strategies pre-fix.

Core thesis matches your discretionary edge: “trend + pullback into EMA zone + continuation.”

C) What Needs Revision
1) ADX “Strong trend” bonus is currently a lie

Right now you add +15 and label “Strong trend” even if ADX is weak. That inflates confidence and lets trash through.

2) Candle confirmation is too dumb (color ≠ quality)

close > open is not confirmation. You want close-in-range quality + reclaim logic (especially reclaim above EMA50).

3) Missing RSI extension veto

Your discretionary filter likely avoids longs when RSI is already cooked (>70), and avoids shorts when RSI is crushed (<30). The bot doesn’t.

4) TP authority ambiguity

You compute takeProfitPrice, and you set preferStructure: true. Depending on how buildDecision() behaves, config can override the explicit TP. Pick one authority.

D) Superior Enhancements
Required

Tiered ADX scoring (truthful)

Replace candle-color with close-in-range quality check

Add “close above EMA50 reclaim” bonus (and mirror for shorts)

Add RSI extension block

Make TP authority deterministic (preferStructure: false OR remove explicit TP)

Optional

Add isRejectionCandle() as a bonus (not gate) to capture “trader’s eye” wick rejection

Add “deep pullback to EMA50” bonus (if not already implicit)

E) Consensus Plan (Ready to Implement)
1) File: EmaPullback.ts
1.1 Replace unconditional ADX +15 with tiered scoring
Where

In both direction blocks, replace the current ADX block:

Long block ADX (currently lines ~79–80)

Short block ADX (currently lines ~92–93)

Current code (long + short, both identical problem)
confidence += 15;
triggers.push(`Strong trend (ADX: ${adxSignal!.toFixed(1)})`);

Replace with tiered logic

Long block: replace the two lines above with:

// ADX TRUTHFUL SCORING (tiered)
if (adxSignal! >= 35) {
  confidence += 15;
  triggers.push(`Very strong trend (ADX: ${adxSignal!.toFixed(1)})`);
} else if (adxSignal! >= 25) {
  confidence += 10;
  triggers.push(`Strong trend (ADX: ${adxSignal!.toFixed(1)})`);
} else if (adxSignal! >= 18) {
  confidence += 5;
  triggers.push(`Moderate trend (ADX: ${adxSignal!.toFixed(1)})`);
} else {
  triggers.push(`Weak trend (ADX: ${adxSignal!.toFixed(1)})`);
  // No bonus: weak trend pullbacks are lower quality
}


Short block: use the exact same snippet.

✅ Result: ADX stops being a “free +15 coupon.”

1.2 Replace candle-color confirmation with close-in-range quality + EMA50 reclaim bonus
Where

Inside each direction block where you currently have:

Long candle check (currently ~lines 84–86)

Short candle check (currently ~lines 97–99)

Current long code
if (signalBar.close > signalBar.open) {
  confidence += 10;
  triggers.push('Bullish candle confirmation');
  reasonCodes.push('CANDLE_CONFIRMATION');
}

Replace long candle code with
// CLOSE-IN-RANGE QUALITY (better than candle color)
const range = signalBar.high - signalBar.low;
const closeRatio = range > 0 ? (signalBar.close - signalBar.low) / range : 0.5;

// Long: close in top 30% of range = quality continuation candle
if (closeRatio >= 0.7) {
  confidence += 15;
  triggers.push('Strong close (top 30% of range)');
  reasonCodes.push('CANDLE_CONFIRMATION');
}

// Long: reclaim above EMA50 is a strong “buyers defended deeper pullback”
if (signalBar.close > ema50Signal!) {
  confidence += 10;
  triggers.push('Strong reclaim above EMA50');
  // (No reason codeA code exists for reclaim; keep as trigger-only)
}

Current short code
if (signalBar.close < signalBar.open) {
  confidence += 10;
  triggers.push('Bearish candle confirmation');
  reasonCodes.push('CANDLE_CONFIRMATION');
}

Replace short candle code with
// CLOSE-IN-RANGE QUALITY (better than candle color)
const range = signalBar.high - signalBar.low;
const closeRatio = range > 0 ? (signalBar.close - signalBar.low) / range : 0.5;

// Short: close in bottom 30% of range = quality continuation candle
if (closeRatio <= 0.3) {
  confidence += 15;
  triggers.push('Strong close (bottom 30% of range)');
  reasonCodes.push('CANDLE_CONFIRMATION');
}

// Short: reclaim below EMA50 is a strong “sellers defended deeper pullback”
if (signalBar.close < ema50Signal!) {
  confidence += 10;
  triggers.push('Strong reclaim below EMA50');
}


✅ Result: your “trader’s eye” starts being encoded: not “green candle,” but where it closed and whether it reclaimed depth.

1.3 Add RSI extension hard block (discretionary veto)
Where

After direction is set and after:

if (!direction) return null;


That is currently around line ~100.

Insert immediately after that line
// RSI EXTENSION VETO (discretionary filter encoded)
// Trend pullbacks are lower quality when RSI is already extended in the trend direction.
if (direction === 'long' && rsiSignal! > 70) return null;
if (direction === 'short' && rsiSignal! < 30) return null;


✅ Result: you stop taking “late trend” pullbacks that feel good on paper and fail in real life.

1.4 Optional: add rejection-candle bonus using existing helper (captures wick rejection)
Where

After the close-in-range check (inside each direction block), add:

const rejection = isRejectionCandle(signalBar, direction);
if (rejection.ok) {
  confidence += 10;
  triggers.push(`Rejection candle bonus (${(rejection.wickRatio * 100).toFixed(0)}% wick)`);
  reasonCodes.push('REJECTION_CONFIRMED');
}


Why optional: You already have multiple quality checks; this adds more selectivity without making it a hard gate.

1.5 TP authority: make it deterministic
Where

In takeProfitConfig, currently:

preferStructure: true,

Change to
preferStructure: false,


✅ This ensures your explicit:

const takeProfitPrice = ...


doesn’t get silently overridden by structure logic.

If you prefer the opposite architecture (config-driven TP), then delete the explicit takeProfitPrice calculation entirely and let config drive exits. But don’t run both.

1.6 Clean up confidence inflation risk (optional but recommended)

You currently clamp confidence:

confidence = Math.max(0, Math.min(100, confidence));


Keep it.

But after adding reclaim + close quality + rejection bonus, your max scores can spike above 100 frequently. That’s fine because of the clamp, but watch for “always passes 50” behavior in testing.

Implementation Checklist

 ADX scoring is tiered (no unconditional +15)

 Candle-color confirmation replaced with close-in-range check

 EMA50 reclaim bonus added (directional)

 RSI extension veto added

 preferStructure: false set (TP authority resolved)

 (Optional) rejection candle bonus added